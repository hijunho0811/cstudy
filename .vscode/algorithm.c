// #define For(i, n) for( int i=0; i<(n); ++i)

// 매크로 사용

#include <stdio.h>
int main()
{
    int n, a, b, cnt;
    int k=0;
    scanf("%d", &n);
    while(k != n)
    {
        a=n/10;
        b=n%10;
        k=
    }
    
}